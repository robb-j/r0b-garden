---
layout: html.njk
---

<header>
  <a href="/">{{ site.title }}</a>
  /
  <a href="/photos/">photos</a>
  /
  {{ title or 'photo' }}
</header>

<main class="photo">
  <center-layout>
    <header class="flow">
      {{ content | safe }}
      <p class="photoMeta">
        {{ date | longDate }} ⸻ ???
      </p>
    </header>
  </center-layout>
  <center-layout>
    <stack-layout>
      {% for slug in media %}
      <figure>
        {% media_image collections.media | findBySlug(slug), 500 %}
        <!--  -->
        <figcaption>{{ media.content | safe }}</figcaption>
      </figure>
      {% endfor %}
    </stack-layout>
  </center-layout>
</main>
