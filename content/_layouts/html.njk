<!doctype html>
<html>
  <head>
    <title>{{ title + ' |' if title }} {{ site.title }}</title>
    <meta charset="utf8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="theme-color" content="{{ meta.color or '#5CB171' }}" />
    <!-- TODO: OpenGraph -->
    <!-- @openlab/alembic inject-css -->
    <link rel="stylesheet" href="https://fonts.openlab.dev/rubik/rubik.css" />
    <link rel="stylesheet" href="/assets/style.css" />
    <style>
      body {
        font-family: Rubik, system-ui, sans-serif;
      }
      figcaption {
        text-align: center;
        font-style: italic;
        margin-bottom: 2em;
      }
    </style>
  </head>
  <body>
    {{ content | safe }}
    <!-- @openlab/alembic inject-js -->
    <!-- TODO: fathom -->
  </body>
</html>
